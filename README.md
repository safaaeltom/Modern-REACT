# Modern React

A React learning repository following a modern React tutorial.

This repository is used to follow along with the tutorial, write the code myself, experiment with the examples, and practice the React concepts introduced throughout the course.

The goal is to build a practical understanding of React while gradually becoming comfortable with React syntax, patterns, and application structure.

## Purpose

This repository is primarily for learning and practice rather than being a finished production application.

It allows me to:

* Follow a structured React tutorial
* Practice the concepts introduced in each lesson
* Experiment with the tutorial code
* Track progress through Git commits
* Revisit previously learned React patterns
* Build familiarity with React syntax through repeated use

## Technologies

* React
* JavaScript
* React Router
* CSS
* JSON Server
* Git & GitHub

## Repository Status

This repository is actively being developed as I progress through the tutorial.

The code may change frequently as new concepts are introduced, previous concepts are revisited, and different approaches are tested.

## React Concepts Covered

As I progress through the tutorial, this repository contains practical examples of concepts including:

* React components
* Props
* State with `useState`
* Controlled inputs
* Event handling
* Rendering lists with `.map()`
* Updating arrays in state
* Conditional rendering
* Data fetching
* Loading and error states
* `useEffect`
* Custom hooks
* React Router
* Navigation with `Link`
* Dynamic routes
* Route parameters with `useParams()`
* Fetching individual resources
* Request cancellation with `AbortController`

The concepts are introduced gradually through the tutorial and practiced directly in the application.

## Current Application

The application currently contains a blog-style section that is used to practice React concepts introduced in the tutorial.

This includes:

* A home page displaying blog posts
* Navigation between pages
* Blog preview links
* Dynamic blog detail routes
* Fetching individual blog posts
* Loading and error handling
* A reusable `useFetch` hook

The blog functionality is part of the tutorial and is not the sole purpose of the repository.

## Project Structure

The project is organized around React components and supporting files introduced throughout the tutorial.

```text
src/
├── App.jsx
├── Navbar.jsx
├── Home.jsx
├── Create.jsx
├── BlogList.jsx
├── BlogDetails.jsx
├── useFetch.jsx
└── index.css
```

The structure may change as new sections of the tutorial are completed.

## Git History

Git commits are used to track progress through the tutorial.

Each meaningful commit represents a feature, React concept, styling change, or other learning milestone. This makes the Git history useful for reviewing how the application developed over time.

## Learning in Progress

This repository is a record of my progress while learning React.

The code is expected to evolve as I learn new concepts, revisit older ones, experiment with different implementations, and become more comfortable writing React independently.
